<odoo>
    <data noupdate="0">

        <!-- Employees: only own records, NO DELETE -->
        <record id="rule_lunch_record_employee" model="ir.rule">
            <field name="name">Lunch Records: Own Records Only</field>
            <field name="model_id" ref="model_lunch_record" />
            <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]" />
            <field name="perm_read" eval="True" />
            <field name="perm_write" eval="True" />
            <field name="perm_create" eval="True" />
            <field name="perm_unlink" eval="False" />
        </record>

        <!-- Admin: full access including DELETE -->
        <record id="rule_lunch_record_admin" model="ir.rule">
            <field name="name">Lunch Records: Admin Full Access</field>
            <field name="model_id" ref="model_lunch_record" />
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('base.group_system'))]" />
            <field name="perm_read" eval="True" />
            <field name="perm_write" eval="True" />
            <field name="perm_create" eval="True" />
            <field name="perm_unlink" eval="True" />
        </record>

    </data>
</odoo>